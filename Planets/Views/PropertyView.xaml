<UserControl x:Class="Planets.Views.PropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Planets.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Planets.ViewModels" mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="10">
            <TextBlock Margin="0,5" Text="All defined properties:" />
            <ListBox Grid.Column="0" HorizontalAlignment="Stretch" DisplayMemberPath="Name"
                     ItemsSource="{Binding Properties}"
                     SelectedItem="{Binding SelectedProperty}" />
        </StackPanel>

        <!--  CRUD buttons  -->
        <StackPanel Grid.Column="1" Margin="0,35" HorizontalAlignment="Left">
            <TextBox Width="200" Text="{Binding EditedPropertyName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <StackPanel Orientation="Horizontal">
                <Button Width="60" Margin="5,5"
                        Command="{Binding AddPropertyCommand}"
                        Content="Add" />
                <Button Width="60" Margin="5,5"
                        Command="{Binding UpdatePropertyCommand}"
                        Content="Update" />
                <Button Width="60" Margin="5,5"
                        Command="{Binding DeletePropertyCommand}"
                        Content="Delete" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>    